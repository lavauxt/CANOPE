% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canope_vcf_export.R
\name{export_canope_to_vcf}
\alias{export_canope_to_vcf}
\title{Export CANOPE CNV calls to VCF}
\usage{
export_canope_to_vcf(
  cnv_calls,
  output_vcf,
  sample_name = NULL,
  source = "CANOPE",
  reference = "hg19"
)
}
\arguments{
\item{cnv_calls}{Data frame of CANOPE CNV calls (the \code{xcnv}-style
table produced by \code{print_cnvs()} / \code{run_canope()}).}

\item{output_vcf}{Output VCF file path.}

\item{sample_name}{Optional character string. If supplied, write a
single-sample VCF for that sample only.}

\item{source}{Source description for the VCF header. Default \code{"CANOPE"}.}

\item{reference}{Reference genome assembly label (e.g. \code{"hg19"}).}
}
\value{
Invisibly returns the path written.
}
\description{
Converts a CANOPE CNV-call data frame (as returned by \code{\link{run_canope}}
and optionally annotated by \code{\link{score_canope_confidence}}) into a
standard VCF file.  When \code{sample_name} is \code{NULL}, a single
multi-sample VCF is written; otherwise a single-sample VCF is produced.
}
