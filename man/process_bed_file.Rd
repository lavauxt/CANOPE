% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canope_bed_process.R
\name{process_bed_file}
\alias{process_bed_file}
\title{Process BED File for CNV Analysis (CANOPE)}
\usage{
process_bed_file(
  input_bed,
  output_bed,
  bed_process = "STANDARD",
  bed_zero_based = TRUE,
  refseqgene = NULL,
  transcripts_file = NULL,
  unknown_gene = FALSE,
  gene_list_restrict = NULL,
  chr_list_restrict = NULL,
  exon_sep = " ",
  gene_name_collapse = " ",
  kmer = NULL,
  customexon = FALSE,
  auto_exon_number = TRUE,
  region_numbering_mode = "bed_text",
  gene_name_keep = NULL,
  list_genes = NULL,
  genes_file = NULL,
  panel_files = NULL,
  genome_version = NULL,
  txdb = NULL,
  gene_field_index = NULL,
  ...
)
}
\arguments{
\item{input_bed}{Path to input BED file.}

\item{output_bed}{Path to output processed BED file.}

\item{bed_process}{Mode: \code{"STANDARD"}, \code{"REGEN"}, or \code{"NO"}.}

\item{bed_zero_based}{Logical: is the input BED 0-based? Default TRUE.}

\item{unknown_gene}{Keep intervals without a gene name? Default FALSE.}

\item{exon_sep}{Character string or vector of characters used to split
gene names (e.g. \code{"[ ()]"} or \code{c(" ", "(")}). Default \code{" "}.}

\item{gene_name_collapse}{Token used to rejoin kept parts. Default \code{" "}.}

\item{customexon}{Add a \code{Custom.Exon} column with exon numbers. Default FALSE.}

\item{auto_exon_number}{If TRUE, assign sequential exon numbers per gene based
on genomic order. If FALSE, fall back to \code{region_numbering_mode}.}

\item{region_numbering_mode}{When \code{auto_exon_number} is FALSE:
\code{"bed_text"} (extract from name string) or \code{"file_order"}.}

\item{gene_name_keep}{Which parts to keep after splitting by \code{exon_sep}
(e.g. \code{"1"}, \code{"1-2"}, \code{"1,3"}).}

\item{panel_files}{Character vector of BED paths (or a file listing paths)
used to annotate gene/exon names onto the input intervals.}

\item{genome_version}{\code{"hg19"} or \code{"hg38"} (REGEN mode only).}

\item{gene_field_index}{1-based field index for the gene name after splitting
(legacy fallback for \code{gene_name_keep}).}

\item{...}{Other parameters, accepted for forward compatibility.}
}
\value{
Invisibly returns \code{output_bed}.
}
\description{
Standardises an input BED file into a clean, gene/exon-annotated BED ready
for \code{\link{run_canope}}. Mirrors ECHO's \code{process_bed_file}: same
three modes, ported as-is so CANOPE users get identical BED-preprocessing
behaviour to ECHO.
}
