% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canope_report.R
\name{generate_canope_report}
\alias{generate_canope_report}
\title{Generate Interactive HTML Report for CANOPE}
\usage{
generate_canope_report(
  rdata_output,
  qc_metrics_file = NULL,
  output_dir,
  settings = NULL,
  sample_table = NULL,
  log_file = NULL,
  prefix = "CANOPE",
  template_path = NULL
)
}
\arguments{
\item{rdata_output}{Path to the CANOPE workspace \code{.RData}
(as written by \code{\link{run_canope}}; must contain
\code{cnv_calls}, \code{counts}, \code{bed_file}, \code{models}, \code{refs}).}

\item{qc_metrics_file}{Optional path to a QC-metrics TSV (from
\code{\link{run_canope_qc_metrics}}). If \code{NULL} or missing, the
report's QC section is skipped gracefully.}

\item{output_dir}{Directory for the rendered HTML (created if missing).}

\item{settings}{Optional named list of confidence-scoring thresholds
(as passed to \code{\link{score_canope_confidence}}) — shown in the
report's "Confidence Score Method" section.}

\item{sample_table}{Optional path to a sample table (TSV with at least
a \code{sample_name} column; \code{gender} optional) for PCA coloring.}

\item{log_file}{Optional path to a pipeline log file to display.}

\item{prefix}{Filename prefix for the output HTML
(\code{CANOPE_<prefix>_report.html}). Default \code{"CANOPE"}.}

\item{template_path}{Optional explicit path to \code{CANOPE_report.Rmd}.
If \code{NULL}, looks for it next to the RData, then in the working
directory, then via
\code{system.file("rmd/CANOPE_report.Rmd", package = "CANOPE")} (the
template's actual location, \code{inst/rmd/}, once installed).}
}
\value{
Invisibly returns the path to the rendered HTML file.
}
\description{
Mirrors ECHO's \code{generate_report()}: loads the CANOPE workspace RData,
optionally a QC-metrics TSV and a sample table, then renders
\code{CANOPE_report.Rmd} with those objects as parameters.
}
