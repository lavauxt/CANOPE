% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canoes_legacy_engine.R
\name{legacy_emission_probs}
\alias{legacy_emission_probs}
\title{Emission Log-Probabilities, Original CANOES Formula}
\usage{
legacy_emission_probs(test_counts, target_means, var_estimate, targets)
}
\arguments{
\item{test_counts}{Integer vector of observed read counts for the test sample (one per target).}

\item{target_means}{Numeric vector of expected read counts (mean) for each target (from reference panel).}

\item{var_estimate}{Numeric vector of variance estimates for each target.}

\item{targets}{Integer vector of target identifiers.}
}
\value{
Matrix with columns \code{target, delprob, normalprob, dupprob} (log-scale).
}
\description{
Port of \code{EmissionProbs()}. Same negative-binomial model as
\code{\link{emission_probs}()} in the modern engine (state means at
0.5x/1x/1.5x of the expected count, size halved/doubled accordingly); the
only behavioural difference is the all-impossible-states edge case, which
the original fills with hard \code{-Inf}/\code{-0.01} sentinels rather than
the softer finite values the modern engine uses.
}
