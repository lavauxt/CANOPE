% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canope_confidence.R
\name{score_canope_confidence}
\alias{score_canope_confidence}
\title{Score CANOPE CNV Calls (HIGH / MEDIUM / LOW)}
\usage{
score_canope_confidence(
  cnv_calls,
  high_q_score = 50L,
  med_q_score = 20L,
  high_num_refs = 10L,
  med_num_refs = 5L,
  high_num_targ = 1L,
  med_num_targ = 1L,
  low_confidence_genes = c("PMS2", "SMN1", "CYP2D6", "HBA1", "HBA2", "STRC", "CYP21A2",
    "GBA1", "CFTR")
)
}
\arguments{
\item{cnv_calls}{Data frame of CANOPE CNV calls.}

\item{high_q_score}{Minimum Q_SOME for HIGH confidence. Default 50.}

\item{med_q_score}{Minimum Q_SOME for MEDIUM confidence. Default 20.}

\item{high_num_refs}{Minimum NUM_REFS for HIGH confidence. Default 10.}

\item{med_num_refs}{Minimum NUM_REFS for MEDIUM confidence. Default 5.}

\item{high_num_targ}{Minimum NUM_TARG for HIGH confidence. Default 1.}

\item{med_num_targ}{Minimum NUM_TARG for MEDIUM confidence. Default 1.}

\item{low_confidence_genes}{Character vector of gene symbols always scored LOW.
Default includes genes with known pseudogene / homology issues.}
}
\value{
The input data frame with an added \code{Confidence} character column
  and a \code{CN_label} column encoding the copy-number state as a string.
}
\description{
Uses the phred-like \code{Q_SOME} quality score, the number of reference
samples (\code{NUM_REFS}), the most-likely copy number (\code{MLCN}),
and a list of genes known to be difficult to score to assign one of three
confidence tiers.
}
\details{
\strong{Assignment logic:}
\enumerate{
  \item HIGH: Q_SOME ≥ \code{high_q_score} AND NUM_REFS ≥ \code{high_num_refs}
        AND MLCN is consistent with CNV type (≤ 1 for DEL, ≥ 3 for DUP)
        AND gene not in \code{low_confidence_genes}.
  \item MEDIUM: Q_SOME ≥ \code{med_q_score} AND NUM_REFS ≥ \code{med_num_refs}
        AND gene not in \code{low_confidence_genes}.
  \item LOW: all other calls, including those in \code{low_confidence_genes}.
}
}
