% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_utils.R
\name{get_coverage_from_bams_megadepth}
\alias{get_coverage_from_bams_megadepth}
\title{Get Per-Target Coverage from BAM Files via Megadepth (fast; native Windows binary)}
\usage{
get_coverage_from_bams_megadepth(
  bam_files,
  bed_input,
  op = c("sum", "mean"),
  threads = 1L,
  work_dir = NULL,
  keep_order = TRUE,
  install_if_missing = TRUE
)
}
\arguments{
\item{bam_files}{Character vector of BAM file paths.}

\item{bed_input}{Path to a BED file of target regions (chrom, start, end, ...).}

\item{op}{\code{"sum"} (default; total covered bases per region --
the closer analogue to a read count) or \code{"mean"} (mean depth).}

\item{threads}{Integer threads per megadepth invocation (BAM decompression).}

\item{work_dir}{Directory for megadepth's intermediate output files.
Default a fresh temp directory.}

\item{keep_order}{Logical. Passes \code{--keep-order} so output rows are
guaranteed to match \code{bed_input}'s row order exactly, regardless of
chromosome order in the BAM. Strongly recommended; default \code{TRUE}.}

\item{install_if_missing}{Logical. If \code{TRUE} (default), calls
\code{megadepth::install_megadepth()} first (a no-op if already installed).}
}
\value{
Data frame of per-target coverage values (rows = BED targets in
  \code{bed_input} order, columns = BAM basenames without extension).
}
\description{
Get Per-Target Coverage from BAM Files via Megadepth (fast; native Windows binary)
}
