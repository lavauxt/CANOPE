# This file is part of the standard testthat infrastructure for an R
# package (tests/testthat.R + tests/testthat/test-*.R). Run the whole suite
# with devtools::test(), or testthat::test_check("CANOPE") after
# devtools::load_all() / library(CANOPE).

library(testthat)
library(CANOPE)

test_check("CANOPE")
